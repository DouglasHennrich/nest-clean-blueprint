import { Column, Entity } from 'typeorm';
import { BaseEntity } from '@/@shared/entities/base.entity';
import { I{{Entity}}Model } from '../models/{{entity}}.struct';

// BaseEntity already provides id, createdAt, updatedAt and deletedAt columns —
// do NOT redeclare them here.
@Entity({ name: '{{entitiesPlural}}' })
export class {{Entity}}Entity extends BaseEntity implements I{{Entity}}Model {
  // TODO: add fields. Enums MUST be stored as VARCHAR.
}
