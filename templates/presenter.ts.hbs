import { AbstractPresenter } from '@/@shared/classes/presenter';
import { I{{Entity}}Model } from '../models/{{entity}}.struct';

export interface I{{Entity}}PresenterResponseModel {
  id: string;
  // TODO: add fields exposed by the API.
  createdAt: string;
  updatedAt: string;
}

export abstract class I{{Entity}}Presenter extends AbstractPresenter<
  I{{Entity}}Model,
  I{{Entity}}PresenterResponseModel
> {}

// No @Injectable(): presenters are wired via `{ provide, useClass }` in the module.
// presentWithoutRelations()/presentMany() come from AbstractPresenter — only
// override them if this presenter needs custom behavior for those cases.
export class {{Entity}}Presenter extends I{{Entity}}Presenter {
  present({
    entity,
  }: {
    entity: I{{Entity}}Model;
    options?: any;
  }): I{{Entity}}PresenterResponseModel {
    return {
      id: entity.id,
      createdAt: entity.createdAt.toISOString(),
      updatedAt: entity.updatedAt.toISOString(),
    };
  }
}
