import { z } from 'zod';

export const {{action}}{{Entity}}DtoSchema = z.object({
  // TODO: define fields for this action's request shape (body, param or query).
});

export type T{{Action}}{{Entity}}Dto = z.infer<typeof {{action}}{{Entity}}DtoSchema>;

// Optional: only add a service-layer variant when the service needs fields
// beyond the request shape (e.g. merging the route `:id` param into the body
// for an update action). See update-order.dto.ts in _example_orders for the
// canonical case. Delete this block when not needed.
//
// export const {{action}}{{Entity}}ServiceDtoSchema = {{action}}{{Entity}}DtoSchema.extend({
//   id: z.string().uuid(),
// });
//
// export type T{{Action}}{{Entity}}ServiceDto = z.infer<
//   typeof {{action}}{{Entity}}ServiceDtoSchema
// >;
